export interface ResizeImageParam {
  sourceFile: string;
  targetFile: string;
  imageWith: number;
  imageHeight: number;
}

export interface ImageParam {
  filename?: string;
  width?: string;
  height?: string;
}
